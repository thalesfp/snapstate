# @snapstore/core

Core class-based reactive stores for [Snapstore](https://github.com/thalesfp/snapstore): typed dot-path state, path-aware subscriptions, structural sharing, and built-in async tracking. No React dependency.

```bash
npm install @snapstore/core
```

See the [full documentation](https://github.com/thalesfp/snapstore#readme).
